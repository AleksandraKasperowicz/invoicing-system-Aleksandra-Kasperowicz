spring:
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: postgres
    password: 4004
    driver-class-name: org.h2.Driver
  flyway:
    enabled: true

  invoicing-system:
    database: sql